spring:
  application:
    name: scf
  datasource:
    url: jdbc:mariadb://localhost:3306/scf_db
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: ed9658pi
  jpa:
    hibernate:
      ddl-auto: update  # Automatically updates the database schema
    show-sql: true  # Shows SQL statements in the console

logging:
  level:
    org:
      hibernate:
        SQL: debug

server:
  error:
    include-message: always
    include-binding-errors: always